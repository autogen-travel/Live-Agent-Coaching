import { Account } from "./account";
export interface CustomerInfo {
    server: string;
    id: string;
    name: string;
    maxConcurrency: number;
}
/**
 * Interface representing information about a feature's availability status
 * Contains the feature name and whether it is enabled for the current customer
 */
export interface FeatureInfo {
    featureName: string;
    isEnabled: boolean;
}
/**
 * Get information about current logged-in user
 */
export declare function getCustomerInfo(options?: {
    account?: Account;
}): Promise<CustomerInfo>;
/**
 * Checks if a specific feature is enabled for the current customer
 */
export declare function checkFeature(featureName?: string, options?: {
    account?: Account;
}): Promise<FeatureInfo>;
