import * as base from "./base";
import * as api from "./generated/core/api";
import * as models from "./generated/core/models";
import { Account } from "../account";
import { Logger } from "../log";
import { TaskStatus } from "./generated/core/models";
export interface SessionLogEntitiesWithCount {
    entites: models.SessionLogResponseDTO[];
    totalCount: number;
}
export declare class SessionLogApi extends base.Api<api.SessionLogApi> {
    constructor(account: Account, log: Logger);
    getSessionEntity(jobId: string): Promise<models.SessionLogResponseDTO>;
    getSessionDebugLog(jobId: string): Promise<Record<string, unknown>[]>;
    getSessionEntities(fromDate: string | undefined, toDate: string | undefined, taskStatus: TaskStatus | undefined, skip: number | undefined, take: number | undefined): Promise<SessionLogEntitiesWithCount>;
}
