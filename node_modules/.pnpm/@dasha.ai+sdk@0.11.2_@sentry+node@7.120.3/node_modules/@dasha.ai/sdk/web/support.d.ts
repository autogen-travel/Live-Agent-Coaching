import { Account } from "./account";
import { TicketAddDTO } from "./rest-api/generated/core";
/**
 * Create a new ticket
 *
 * @returns nothing
 */
export declare function createTicket(ticket: TicketAddDTO, options?: {
    account?: Account;
}): Promise<void>;
