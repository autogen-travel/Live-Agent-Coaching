import { Logger } from "./log";
import http from "http";
export declare function readGetSearchParams(request: http.IncomingMessage): Record<string, string>;
export declare function readPostParamsJson(req: http.IncomingMessage): Promise<any>;
export declare class HttpServer extends http.Server {
    private _router;
    private _descriptions;
    protected _log: Logger;
    allowCors: boolean;
    constructor(log: Logger);
    get(route: string, listener: http.RequestListener, description?: string): this;
    post(route: string, listener: http.RequestListener, description?: string): this;
    getApiDescription(): string;
    private _requestListener;
}
