import { FileSystemWrapper } from "./file-system/file-system-wrapper";
/**
 * Attention prepare directory before unzip
 */
export declare function unzip(directoryPath: string, buffer: Buffer, fs: FileSystemWrapper): Promise<void>;
