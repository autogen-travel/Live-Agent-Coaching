import { Account } from "../account";
import { Logger } from "../log";
import * as base from "./base";
import * as api from "./generated/core/api";
import * as models from "./generated/core/models";
export declare class CustomerApi extends base.Api<api.CustomerApi> {
    constructor(account: Account, log: Logger);
    getMe(): Promise<models.CustomerResponseDTO>;
    /**
     * Checks if a specific feature is enabled for the current customer
     * @param featureName Name of the feature to check
     * @returns Customer feature response data
     */
    checkFeature(featureName?: string): Promise<models.CustomerFeatureCheckDTO>;
}
