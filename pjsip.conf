; Auto generated from config 'test configuration' for dasha.ai 'agent-training-demo' in group 'Default'
; replate <password> to your actual password

[to-dasha-test configuration]
type = aor
contact = sip:049bfb0d-b611-4879-9219-f034a52bcde1@sip.us.dasha.ai

[to-dasha-test configuration]
type = auth
auth_type = userpass
username = 049bfb0d-b611-4879-9219-f034a52bcde1
password = <password>

[to-dasha-test configuration]
type = endpoint
outbound_auth = to-dasha-test configuration
aors = to-dasha-test configuration
context = to-dasha-test configuration
disallow = all
allow = alaw
allow = ulaw

; END of auto-generated config
; for extensions.conf, uncomment lines bellow and replace <your-trunk> to your PSTN provide trunk name
; [some-exten]
; exten => _X.,1,Verbose(0, "Calling from '${CALLERID(num)}' to '${EXTEN}'")
; same  => n,Dial(PJSIP/to-dasha-test configuration,60)
; same  => n,Hangup()